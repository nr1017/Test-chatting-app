import React from 'react';
import styled from 'styled-components';

const Chat = () => {
  return <ChatWrapper>Chat</ChatWrapper>;
};

export default Chat;

const ChatWrapper = styled.div`
  flex: 2;
`;
